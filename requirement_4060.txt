# =============================================================================
# requirements_4060.txt - Dark Vessel SR-YOLO (RFDN Only)
# =============================================================================
# RTX 4060 환경용 - mamba_ssm 제외
# 
# 설치:
#   pip install -r requirements_4060.txt
# =============================================================================

# PyTorch (CUDA 12.1 기준, 환경에 맞게 조정)
torch>=2.0.0
torchvision>=0.15.0

# YOLO
ultralytics>=8.0.0

# Image Processing
opencv-python>=4.8.0
Pillow>=9.0.0

# Scientific Computing
numpy>=1.24.0
scipy>=1.10.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# Logging & Visualization
tensorboard>=2.13.0
matplotlib>=3.7.0

# (Optional) Jupyter
# jupyter>=1.0.0
# ipywidgets>=8.0.0

# =============================================================================
# NOTE: mamba-ssm 제외됨 (4090 PC에서만 사용)
# MambaSR 사용하려면:
#   pip install mamba-ssm causal-conv1d
# =============================================================================