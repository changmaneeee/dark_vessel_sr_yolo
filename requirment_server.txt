# =============================================================================
# requirements_server.txt - AIS-SAT-PIPELINE (Server/Pyenv Env)
# =============================================================================
# Python 3.8 ~ 3.10 권장
# 설치: pip install -r requirements_server.txt
# =============================================================================

# 1. Core Deep Learning (CUDA 버전 확인 필요)
torch>=2.0.0
torchvision>=0.15.0

# 2. Object Detection & SR
ultralytics>=8.0.0
scipy>=1.10.0       # SR 모델 연산용

# 3. Image Processing
# 서버(Headless) 환경에서는 opencv-python-headless가 더 안정적일 수 있음
# 만약 cv2 import 에러 발생 시: pip install opencv-python-headless
opencv-python>=4.8.0
Pillow>=10.0.0
scikit-image>=0.21.0

# 4. Data Handling & Metrics
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0  # F1 Score, Precision 등 계산용

# 5. Configuration & Utils
PyYAML>=6.0
omegaconf>=2.3.0
tqdm>=4.66.0

# 6. Logging & Visualization
tensorboard>=2.14.0
matplotlib>=3.7.0
seaborn>=0.12.0

# 7. Server Monitoring (Optional)
psutil>=5.9.0       # CPU/RAM 모니터링
# nvidia-ml-py3>=7.352.0  # GPU 모니터링 (필요시 주석 해제)

# =============================================================================
# [Optional] Mamba Architecture (서버 GPU가 지원하는 경우)
# mamba-ssm>=1.0.1
# causal-conv1d>=1.0.0
# =============================================================================